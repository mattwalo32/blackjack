#pragma once

#include <string>

namespace GameConstants {
	const int DEFAULT_WINDOW_WIDTH = 1024;
	const int DEFAULT_WINDOW_HEIGHT = 768;

	const std::string SERVER_URL = "ws://localhost/ws";
}